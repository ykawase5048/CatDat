id: B
name: category of finite sets and bijections
notation: $\IB$
objects: finite sets
morphisms: bijective maps
description: This category is also known as the permutation groupoid. It appears in the definition of a combinatorial species.
nlab_link: https://ncatlab.org/nlab/show/permutation+groupoid

tags:
  - combinatorics
  - set theory

related_categories:
  - FI
  - FS

satisfied_properties:
  - property: locally small
    proof: There is a forgetful functor $\IB \to \Set$ and $\Set$ is locally small.

  - property: locally finite
    proof: There is a faithful functor $\IB \to \FinSet$ and <a href="/category/FinSet">$\FinSet$</a> is locally finite.

  - property: inhabited
    proof: This is trivial.

  - property: groupoid
    proof: This is trivial.

  - property: essentially countable
    proof: Every finite set is isomorphic to some $\{1,\dotsc,n\}$ for some $n \in \IN$.

unsatisfied_properties:
  - property: small
    proof: Even the collection of singletons is not small.

  - property: skeletal
    proof: This is trivial.

  - property: generator
    proof: This is trivial.

  - property: essentially finite
    proof: This is trivial.

  - property: countable
    proof: This is trivial.

  - property: connected
    proof: For every $n \geq 0$ there is a connected component of sets of size $n$.

special_objects: {}

special_morphisms:
  isomorphisms:
    description: every morphism
    proof: It is a groupoid by construction.
  monomorphisms:
    description: every morphism
    proof: This is trivial.
  epimorphisms:
    description: every morphism
    proof: This is trivial.
  regular monomorphisms:
    description: same as monomorphisms
    proof: This is because the category is mono-regular.
  regular epimorphisms:
    description: same as epimorphisms
    proof: This is because the category is epi-regular.
