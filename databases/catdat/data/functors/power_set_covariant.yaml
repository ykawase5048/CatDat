id: power_set_covariant
name: covariant power set functor
notation: $P$
source: Set
target: Set
description: 'This functor maps a set $X$ to its power set $P(X)$ and a map of sets $f : X \to Y$ to the induced image operator $f_* : P(X) \to P(Y)$.'
nlab_link: https://ncatlab.org/nlab/show/power+set

tags:
  - set theory

related_functors:
  - power_set_contravariant

satisfied_properties:
  - property: faithful
    proof: 'Let $f,g : X \rightrightarrows Y$ be two maps with $f_* = g_* : P(X) \rightrightarrows P(Y)$. Then $\{f(x)\} = f_*(\{x\}) = g_*(\{x\}) = \{g(x)\}$ and hence $f(x) = g(x)$ for all $x \in X$.'

  - property: preserves epimorphisms
    proof: 'If $f : X \to Y$ is surjective, then $f_* \circ f^* = \id_{P(Y)}$, so that $f^*$ is surjective.'

  - property: preserves monomorphisms
    proof: 'If $f : X \to Y$ is injective, then $f^* \circ f_* = \id_{P(X)}$, so that $f_*$ is injective.'

  - property: conservative
    proof: 'Assume that $f : X \to Y$ is a map such that $f_* : P(X) \to P(Y)$ is an isomorphism. There is some $A \subseteq X$ with $Y = f_*(A)$, this proves that $f$ is surjective. It is also injective: If $x,y \in X$ satisfy $f(x) = f(y)$, then $f_*(\{x\}) = f_*(\{y\})$, and hence $\{x\} = \{y\}$, i.e. $x = y$.'

  - property: preserves coreflexive equalizers
    proof: >-
      Let $f,g : X \rightrightarrows Y$ be a coreflexive pair in $\Set$. Choose a common retraction $r : Y \to X$, so that $rf = rg = \id_X$. Let $E = \{x \in X : f(x)=g(x)\}$ be the usual equalizer in $\Set$. We must show that for every subset $A \subseteq X$, the equality $f_*(A) = g_*(A)$ implies $A \subseteq E$.
      To see this, let $a \in A$. Then $f(a)=g(a')$ for some $a' \in A$. Applying $r$ to both sides yields $a=a'$. Hence $f(a)=g(a)$, so $a \in E$.

unsatisfied_properties:
  - property: essentially surjective
    proof: Every power set is non-empty.

  - property: preserves initial objects
    proof: We have $2^0 \neq 0$.

  - property: preserves terminal objects
    proof: We have $2^1 \neq 1$.

  - property: full
    proof: Take any map $P(X) \to P(X)$ that does not preserve the empty set, say the constant map with value $X$ (for $X \neq \varnothing$).

  - property: preserves reflexive coequalizers
    proof: >-
      Consider the two maps $f,g : \IN \times \{0,1\} \rightrightarrows \IN$ defined by $f(n,i)=n$, $g(n,0)=n$, and $g(n,1)=n+1$. They form a reflexive pair with common section $n \mapsto (n,0)$. Their coequalizer is the quotient of $\IN$ by the equivalence relation generated by $n \sim n+1$, which collapses everything to a single point. Thus, the unique map $p : \IN \to \{\ast\}$ is a coequalizer of $f,g$.
      The induced map $P(p) : P(\IN) \to P(\{\ast\}) = \{\varnothing,\{\ast\}\}$ sends $\varnothing$ to $\varnothing$ and every non-empty subset to $\{\ast\}$. In particular, all non-empty subsets of $\IN$ have the same image.

      Next, we compute the coequalizer of $P(f),P(g) : P(\IN \times \{0,1\}) \rightrightarrows P(\IN)$. Identify $P(\IN \times \{0,1\})$ with $P(\IN) \times P(\IN)$. Under this identification, $P(f)$ sends a pair of subsets $(A,B)$ to $A \cup B$, while $P(g)$ sends it to $A \cup (B+1)$.

      The coequalizer is the quotient of $P(\IN)$ by the equivalence relation generated by
      $$A \cup B \sim A \cup (B+1).$$
      This generated equivalence relation is the transitive closure of the symmetric closure. From this it follows that a finite subset is equivalent only to finite subsets. In particular, $\{0\}$ and $\IN$ are not equivalent, i.e. they do not have the same image in the coequalizer. Therefore, $P(p)$ cannot be the coequalizer.

  - property: preserves equalizers
    proof: 'Any pair of distinct surjective maps $f,g : X \rightrightarrows Y$ provides a counterexample: Their equalizer $E$ is a proper subset of $X$, so that $P(E)$ cannot contain $X$. But $f_*(X) = Y = g_*(X)$ shows that $X$ is contained in the equalizer of $f_*,g_* : P(X) \rightrightarrows P(Y)$.'

  - property: finitary
    proof: The filtered colimit $\IN = \bigcup_{n \geq 0} \IN_{\leq n}$ is not preserved by $P$, since $\bigcup_{n \geq 0} P(\IN_{\leq n})$ just consists of the <i>finite</i> subsets of $\IN$.

  - property: cofinitary
    proof: 'The canonical map $P(\lim_i X_i) \to \lim_i P(X_i)$ is typically not injective already for cardinality reasons: Consider for example the sequential diagram $X_n \coloneqq \{0,1\}^n$ with the truncation maps $X_{n+1} \to X_n$. The limit is $\{0,1\}^{\IN}$ with cardinality $c \coloneqq 2^{\aleph_0}$, so that its power set has cardinality $2^c$. However, the limit of the power sets $P(X_n)$ is a subset of a countable product of finite sets, hence has cardinality $\leq c$, which is strictly smaller than $2^c$.'

  - property: left-invertible
    proof: >-
      Assume that there is a functor $F : \Set \to \Set$ with natural isomorphisms
      $$\alpha_X : X \to F(P(X)).$$
      By the Yoneda Lemma, there is some element $u \in F(P(1))$ (where $1$ is the singleton set as usual) such that $\alpha_X$ is given by
      $$\alpha_X(x) = F(P(x : 1 \to X))(u).$$
      The map $P(x) : P(1) \to P(X)$ is given by $\varnothing \mapsto \varnothing$ and $1 \mapsto \{x\}$. It factors through the map $e : X + 1 \to P(X)$ which maps $a \in X$ to $\{a\}$ and the unique element of $1$ to $\varnothing$. Since $\alpha_X$ is surjective, it follows that also $F(e) : F(X + 1) \to F(P(X))$ is surjective. Since $e$ is a split monomorphism, also $F(e)$ is a split monomorphism. Hence, $F(e)$ must be an isomorphism. But then $F(X+1) \cong F(P(X)) \cong X$ for all sets $X$. If $X$ is a finite non-empty set, we conclude $\card(F(X)) = \card(X) - 1$. But then
      $$\card(X) = \card(F(P(X))) = \card(P(X)) - 1 = 2^{\card(X)} - 1$$
      yields a contradiction when $\card(X) > 1$.

undecidable_properties:
  - property: essentially injective
    proof: >-
      The property $P(X) \cong P(Y) \implies X \cong Y$ is undecidable in ZFC. It holds when the generalized continuum hypothesis is true, but it can fail otherwise. A more precise result is given by <a href="https://en.wikipedia.org/wiki/Easton's_theorem" target="_blank">Easton's Theorem</a>. See also <a href="https://mathoverflow.net/questions/17152" target="_blank">MO/17152</a>.
